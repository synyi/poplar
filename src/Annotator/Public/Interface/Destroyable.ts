// After seeing this, U may know
// which language is my first learnt language
// in fact it's my second language
// after all, those two languages has very close relationship
/**
 * 可被删除的
 */
export interface Destroyable {
    destructor();
}